import { colors } from './colors';
import { fonts, fontSizes } from './fonts';
import { spaces, lineHeights } from './spaces';

export const theme = {
  colors,
  fonts,
  fontSizes,
  spaces,
  lineHeights,
};
